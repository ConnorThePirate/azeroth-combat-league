{
  "name": "@acl/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Versioned wire contracts: canonical JSON, hashing, WFP2/WFU1 envelopes, validators, fixtures.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "fixtures": "node tools/gen-fixtures.mjs"
  },
  "dependencies": {
    "@noble/hashes": "1.8.0",
    "fflate": "0.8.3"
  },
  "devDependencies": {
    "@types/node": "20.19.0",
    "typescript": "5.9.3",
    "vitest": "5.0.1"
  }
}
